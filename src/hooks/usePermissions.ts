// This hook is deprecated - use usePermissions from PermissionContext instead
// Keeping empty to avoid breaking imports
export const usePermissions = () => {
  // Do nothing - permissions now handled by PermissionContext
};
