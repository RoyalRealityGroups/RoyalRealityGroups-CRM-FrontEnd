name: RRG Production Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install Dependencies
        run: npm install --force

      - name: Build React App
        env:
          VITE_API_BASE_URL: ${{ secrets.VITE_API_BASE_URL }}
          VITE_API_TIMEOUT: ${{ secrets.VITE_API_TIMEOUT }}
          VITE_APP_NAME: ${{ secrets.VITE_APP_NAME }}
          VITE_APP_VERSION: ${{ secrets.VITE_APP_VERSION }}
          VITE_ENV: ${{ secrets.VITE_ENV }}
        run: npm run build

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: react-production-build
          path: dist
          if-no-files-found: error
          retention-days: 30

      # - name: Create ZIP
      #   run: zip -r react-build.zip dist

      # - name: Copy ZIP to Server
      #   uses: appleboy/scp-action@v0.1.7
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SERVER_SSH_KEY }}
      #     port: ${{ secrets.SERVER_PORT }}
      #     source: "react-build.zip"
      #     target: "/home/${{ secrets.SERVER_USER }}/"

      # - name: Deploy on Server
      #   uses: appleboy/ssh-action@v1.2.0
      #   with:
      #     host: ${{ secrets.SERVER_HOST }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SERVER_SSH_KEY }}
      #     port: ${{ secrets.SERVER_PORT }}
      #     script: |
      #       mkdir -p /var/www/my-react-app
      #       rm -rf /var/www/my-react-app/*
      #       unzip -o /home/${{ secrets.SERVER_USER }}/react-build.zip -d /tmp/react-build
      #       cp -r /tmp/react-build/dist/* /var/www/my-react-app/
      #       rm -rf /tmp/react-build
      #       rm -f /home/${{ secrets.SERVER_USER }}/react-build.zip
